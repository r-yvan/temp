import CMD from '@/components/cmd/CMD';
import React from 'react';

export const metadata = {
  title: 'RCA | CMD',
  description: "It's the terminal",
};

const HBD = () => {
  return (
    <div className="">
      <CMD />
    </div>
  );
};

export default HBD;
