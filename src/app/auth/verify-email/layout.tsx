import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Verify Email - RCAMIS',
  description: 'verify your email',
};

const VerifyLayout = ({ children }: any) => {
  return children;
};

export default VerifyLayout;
