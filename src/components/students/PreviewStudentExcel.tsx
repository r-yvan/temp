import React, { FC } from 'react';
import { Table } from '@mantine/core';

interface Props {
  data: any;
}

const PreviewStudentExcel: FC<Props> = ({ data }) => {
  return <div></div>;
};

export default PreviewStudentExcel;
